# Task 6 Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix critical integration issues in Task 6 atomic purchase implementation

**Architecture:** Update SQL function schema, integrate buyItem into purchase flow, add validation

**Tech Stack:** PostgreSQL, Supabase RPC, JavaScript

## Global Constraints

- Fix schema mismatch (player_id not user_id)
- Integrate atomic purchase into existing flow
- Maintain backward compatibility
- No breaking changes to UI
- All existing features must continue working

---

### Task 1: Fix RPC Function Schema

**Files:**
- Modify: `docs/supabase-rpc-functions.sql`
- Modify: `docs/TASK-6-SETUP-INSTRUCTIONS.md`

**Interfaces:**
- Consumes: existing SQL function with wrong schema
- Produces: corrected SQL function matching actual player_items table

- [ ] **Passo 1: Corrigir INSERT no RPC function**

Editar `docs/supabase-rpc-functions.sql`, encontrar o INSERT (linha 34) e substituir por:

```sql
-- Adicionar item ao inventário
INSERT INTO player_items (player_id, item_id, item_type, acquired_at)
VALUES (p_user_id, p_item_id, p_item_type, NOW())
RETURNING * INTO purchased_item;
```

Mudanças:
- `user_id` → `player_id` para match com schema real
- Mantém `item_type`, `item_id`, `acquired_at`
- Não adiciona `uses_remaining` ou `is_permanent` (são gerenciados pelo app, não RPC)

- [ ] **Passo 2: Atualizar documentação**

Editar `docs/TASK-6-SETUP-INSTRUCTIONS.md`, adicionar seção "Schema Requirements" antes dos testes:

```markdown
## Schema Requirements

Before running the SQL function, verify your `player_items` table has these columns:
- `player_id` (UUID, references players.id)
- `item_id` (TEXT)
- `item_type` (TEXT)
- `acquired_at` (TIMESTAMP WITH TIME ZONE)

Run in Supabase SQL Editor to verify:
```sql
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'player_items';
```
```

- [ ] **Passo 3: Commit**

```bash
git add docs/supabase-rpc-functions.sql docs/TASK-6-SETUP-INSTRUCTIONS.md
git commit -m "fix(shop): correct player_items schema in RPC function

- Change user_id to player_id to match actual database schema
- Add schema verification instructions
- Ensure INSERT uses correct column names

Fixes schema mismatch that would cause RPC function to fail on deployment."
```

---

### Task 2: Integrate Atomic Purchase

**Files:**
- Modify: `src/classes/ShopClass.js` (replace purchaseItem method)

**Interfaces:**
- Consumes: old purchaseItem method (line 404-550)
- Produces: atomic purchaseItem using buyItem internally

- [ ] **Passo 1: Adicionar validação de item no buyItem**

Editar `src/classes/ShopClass.js`, encontrar método `buyItem` (linha 332) e adicionar validação ANTES do check isPurchasing:

```javascript
async buyItem(itemId, itemType, price) {
    // Validar item existe
    const item = this.getItemById(itemId);
    if (!item) {
        console.error('❌ Item não encontrado:', itemId);
        alert('Item não encontrado');
        return { success: false, error: 'Item não encontrado' };
    }

    // Usar dados reais do item
    const actualPrice = item.price;
    const actualType = item.category;

    // Prevenir double-submit
    if (this.isPurchasing) {
        console.log('⏳ Compra já em andamento...');
        return { success: false };
    }

    this.isPurchasing = true;
    // ... resto do código
```

- [ ] **Passo 2: Usar preço e tipo corretos do item**

Ainda no método `buyItem`, atualizar a chamada RPC (linha ~358) para usar valores validados:

```javascript
const { data, error } = await supabase.rpc('atomic_purchase', {
    p_user_id: currentUser.id,
    p_item_id: itemId,
    p_item_type: actualType,  // MUDANÇA: usar tipo real
    p_price: actualPrice      // MUDANÇA: usar preço real
});
```

- [ ] **Passo 3: Adicionar tratamento de skin após compra**

Ainda no método `buyItem`, ANTES do return success (linha ~380), adicionar:

```javascript
if (data.success) {
    // Atualizar moedas localmente
    currentUser.coins = data.remaining_coins;
    this.rankingManager.setCurrentUser(currentUser);

    // Auto-selecionar skin se for uma skin
    if (item.category === 'skins') {
        this.setSelectedSkin(itemId, item.skinFile, item.name);
    }

    console.log('✅ Compra concluída:', {
        item: item.name,
        newBalance: data.remaining_coins
    });

    return {
        success: true,
        message: `${item.name} comprado com sucesso!`,
        remainingCoins: data.remaining_coins
    };
}
```

- [ ] **Passo 4: Substituir implementação do purchaseItem**

Encontrar o método `purchaseItem` (linha ~404) e substituir TODO o corpo por:

```javascript
async purchaseItem(itemId) {
    const currentUser = this.rankingManager.getCurrentUser();
    if (!currentUser) {
        alert('Você precisa estar logado para comprar!');
        return { success: false, error: 'Usuário não autenticado' };
    }

    // Obter item
    const item = this.getItemById(itemId);
    if (!item) {
        alert('Item não encontrado!');
        return { success: false, error: 'Item não encontrado' };
    }

    // Validar tipo de item
    if (item.disabled || item.comingSoon) {
        alert('Este item ainda não está disponível!');
        return { success: false, error: 'Item indisponível' };
    }

    // Tratamento especial para coin packs (PIX)
    if (item.category === 'coin_packs' && item.priceType === 'real') {
        return await this.purchaseCoinPack(item, currentUser);
    }

    // Usar buyItem para compra atômica
    return await this.buyItem(itemId, item.category, item.price);
}
```

Mudanças:
- Remove TODA lógica de validação manual
- Remove TODA lógica de UPDATE/INSERT manual
- Delega para `buyItem` que usa RPC atômica
- Mantém tratamento de coin packs (PIX)

- [ ] **Passo 5: Testar integração**

Testes manuais:
1. Abrir `shop.html`
2. Fazer login com usuário de teste
3. Tentar comprar uma skin barata
4. Verificar que:
   - Moedas foram deduzidas
   - Item aparece no inventário
   - Skin foi selecionada automaticamente
   - Console mostra logs do buyItem
5. Tentar comprar item caro sem moedas
   - Deve mostrar erro de saldo insuficiente
6. Clicar rapidamente 5 vezes no mesmo item
   - Apenas 1 compra deve ser processada

- [ ] **Passo 6: Commit**

```bash
git add src/classes/ShopClass.js
git commit -m "fix(shop): integrate atomic purchase into main flow

- Add item validation in buyItem method
- Use actual item price and category instead of trusting parameters
- Add skin auto-selection after purchase
- Persist user coin balance via rankingManager
- Replace purchaseItem implementation with atomic buyItem call
- Maintain special handling for coin packs (PIX)

Completes Task 6 by actually using the atomic RPC function for all
purchases. Fixes critical bug where separate UPDATE/INSERT could fail
partially, causing coin loss without item credit.

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---
